# Allgemeine App-Einstellungen
app:
  name: "Antigravity Trading Platform"
  env: "development"

# Feature Flags
flags:
  use_mock_data: true # Wenn true: Lade Daten aus fixtures/ statt echte API-Calls

# Admin Konfiguration
admin:
  log_level: "INFO"
  report_language: "de"
